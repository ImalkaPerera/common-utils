name: Build and Deploy common-utils

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup Java
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '17'

      # 1) Create truststore in WORKSPACE (always writable)
      - name: Create truststore (workspace)
        run: |
          TRUSTSTORE="$GITHUB_WORKSPACE/nexus-truststore.jks"

          # Copy default cacerts (keeps Maven Central working)
          cp "$JAVA_HOME/lib/security/cacerts" "$TRUSTSTORE"

          # Verify it exists and is writable
          ls -l "$TRUSTSTORE"
          test -w "$TRUSTSTORE"

          # Write Nexus cert
          echo "${{ secrets.NEXUS_CERT }}" > "$GITHUB_WORKSPACE/nexus.crt"

          # Import Nexus cert
          keytool -import -trustcacerts \
            -alias nexus \
            -file "$GITHUB_WORKSPACE/nexus.crt" \
            -keystore "$TRUSTSTORE" \
            -storepass changeit \
            -noprompt

          # Export for next steps
          echo "TRUSTSTORE=$TRUSTSTORE" >> $GITHUB_ENV

      # 2) Configure Maven credentials
      - name: Configure Maven settings.xml
        run: |
          mkdir -p ~/.m2
          cat <<EOF > ~/.m2/settings.xml
          <settings>
            <servers>
              <server>
                <id>nexus-snapshots</id>
                <username>${NEXUS_USER}</username>
                <password>${NEXUS_TOKEN}</password>
              </server>
              <server>
                <id>nexus-releases</id>
                <username>${NEXUS_USER}</username>
                <password>${NEXUS_TOKEN}</password>
              </server>
            </servers>
          </settings>
          EOF
        env:
          NEXUS_USER: ${{ secrets.NEXUS_USER }}
          NEXUS_TOKEN: ${{ secrets.NEXUS_TOKEN }}

      # 3) Build + Deploy with explicit truststore
      - name: Build and Deploy
        run: |
          cd "Maven Project"
          mvn clean deploy -U \
            -Djavax.net.ssl.trustStore="$TRUSTSTORE" \
            -Djavax.net.ssl.trustStorePassword=changeit